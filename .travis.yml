language: node_js
node_js:
  - "10"

cache:
  directories:
    - node_modules

branches:
  only: 
    - master

install:
  - yarn
  - yarn global add webpack-cli@3.1.2 webpack@4

script:
  - yarn build
  - yarn test
